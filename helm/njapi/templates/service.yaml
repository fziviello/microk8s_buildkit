apiVersion: v1
kind: Service
metadata:
  name: {{ include "njapi.fullname" . }}
  labels:
    {{- include "njapi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: {{ .Values.service.targetPort }}
      protocol: TCP
      name: http
  selector:
    app.kubernetes.io/name: {{ include "njapi.name" . }}
    app.kubernetes.io/instance: {{ .Release.Name }}